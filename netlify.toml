[build]
  publish = "."
  functions = "netlify/functions"

# So the front-end can keep calling fetch("/api/contact") unchanged —
# Netlify rewrites it to the actual function URL under the hood.
[[redirects]]
  from = "/api/contact"
  to = "/.netlify/functions/contact"
  status = 200
  force = true
